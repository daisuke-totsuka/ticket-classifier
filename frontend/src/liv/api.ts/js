// frontend/src/apiClient.js
const BASE = process.env.REACT_APP_API_BASE || ""; // devは http://localhost:8000、prodは同一オリジン
export const getHealth = () => fetch(`${BASE}/api/health`).then(r => r.json());
